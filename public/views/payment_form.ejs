<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>บันทึกการชำระเงิน</title>
</head>
<body>
    <div class="container">
        <h1>💰 บันทึกการชำระเงิน (Payment)</h1>
        <p>สำหรับงานซ่อมรหัส: <strong><%= repair_id %></strong></p>
        
        <form action="/api/payments" method="POST">
            <input type="hidden" name="repair_id" value="<%= repair_id %>">

            <label>ยอดรวมค่าซ่อมทั้งหมด (Total Cost)</label>
            <input type="number" name="total_cost" step="0.01" placeholder="0.00" required 
                   style="width: 100%; padding: 12px; font-size: 1.2rem; border: 2px solid #ea580c; border-radius: 5px;">

            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px; margin-top: 15px;">
                <div>
                    <label>วันที่ชำระเงิน (Payment Date)</label>
                    <input type="date" name="payment_date" required style="width: 100%; padding: 10px;">
                </div>
                <div>
                    <label>สถานะการชำระเงิน (Status)</label>
                    <select name="payment_status" required style="width: 100%; padding: 10px;">
                        <option value="Pending">🕒 Pending (รอการชำระ)</option>
                        <option value="Paid">✅ Paid (ชำระเงินแล้ว)</option>
                        <option value="Cancelled">❌ Cancelled (ยกเลิก)</option>
                    </select>
                </div>
            </div>

            <div style="margin-top: 30px; display: flex; gap: 10px;">
                <button type="submit" style="flex: 2; background-color: #059669; color: white; font-weight: bold;">
                    💾 บันทึกข้อมูลการเงิน
                </button>
                <a href="/dashboard" class="btn-back" style="flex: 1; text-align: center; text-decoration: none; line-height: 40px;">
                    กลับหน้าหลัก
                </a>
            </div>
        </form>
    </div>
</body>
</html>