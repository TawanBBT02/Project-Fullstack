<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>แก้ไขข้อมูลลูกค้า</title>
</head>
<body>
    <div class="container">
        <h1>📝 แก้ไขข้อมูลลูกค้า</h1>
        <form action="/update-customer/<%= customer.customer_id %>" method="POST">
            
            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                <div>
                    <label>ชื่อ (First Name)</label>
                    <input type="text" name="first_name" value="<%= customer.first_name %>" required>
                </div>
                <div>
                    <label>นามสกุล (Last Name)</label>
                    <input type="text" name="last_name" value="<%= customer.last_name %>" required>
                </div>
            </div>

            <label>เบอร์โทรศัพท์ (Phone)</label>
            <input type="tel" name="phone" value="<%= customer.phone %>" required>

            <label>อีเมล (Email)</label>
            <input type="email" name="email" value="<%= customer.email %>">

            <label>ที่อยู่ (Address)</label>
            <textarea name="address" rows="3"><%= customer.address %></textarea>

            <div style="margin-top: 20px; display: flex; gap: 10px;">
                <button type="submit" style="flex: 2;">💾 บันทึกการแก้ไข</button>
                <a href="/" class="btn-back" style="flex: 1; text-align: center; text-decoration: none; line-height: 45px;">ยกเลิก</a>
            </div>
        </form>
    </div>
</body>
</html>