<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>ระบบจัดการลูกค้า</title>
</head>
<body>
    <div class="container">
        <h1>👥 รายชื่อลูกค้าทั้งหมด</h1>
        <div style="margin-bottom: 20px; text-align: right;">
            <a href="/create-customer" class="btn-detail">➕ เพิ่มลูกค้าใหม่</a>
        </div>

<div class="table-responsive">
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>ชื่อ-นามสกุล</th>
                <th>จัดการ</th>
            </tr>
        </thead>
        
        <tbody>
            <% customers.forEach(customer => { %>
                <tr>
                    <td><%= customer.customer_id %></td> 
                    <td><%= customer.first_name %> <%= customer.last_name %></td>
                    <td>
                        <a href="/update-customer/<%= customer.customer_id %>" class="status progress">แก้ไข</a>
                        <a href="/delete-customer/<%= customer.customer_id %>" class="status failed" onclick="return confirm('ยืนยันการลบ?')">ลบ</a>
                    </td>
                </tr>
            <% }) %>
            </tbody>
    </table>
</div>
<div style="margin-top: 20px; display: flex; gap: 10px;">   
                <a href="/" class="btn-back" style="flex: 1; text-align: center; text-decoration: none; line-height: 40px; background: #ccc; border-radius: 4px; color: black;">กลับหน้าหลัก</a>
            </div>
</body>
</html>