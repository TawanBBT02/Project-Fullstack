<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>รายการช่างเทคนิค</title>
</head>
<body>
    <div class="container">
        <h1>👨‍🔧 รายการช่างเทคนิคทั้งหมด</h1>
        <div style="margin-bottom: 20px; text-align: right;">
            <a href="/add-technician" class="btn-detail" style="background-color: #f59e0b;">➕ เพิ่มช่างเทคนิคใหม่</a>
        </div>

        <div class="table-responsive">
            <table>
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>ชื่อ-นามสกุล</th>
                        <th>เบอร์โทร</th>
                        <th>วันที่เริ่มงาน</th>
                        <th>จัดการ</th>
                    </tr>
                </thead>
                <tbody>
                    <% technicians.forEach(tech => { %>
                    <tr>
                        <td><%= tech.technician_id %></td>
                        <td><%= tech.first_name %> <%= tech.last_name %></td>
                        <td><%= tech.phone %></td>
                        <td><%= tech.hire_date %></td>
                        <td>
                            <a href="/technician/update/<%= tech.technician_id %>" class="status progress">แก้ไข</a>
                            <a href="/technician/delete/<%= tech.technician_id %>" class="status failed" onclick="return confirm('ลบข้อมูลช่างรายนี้?')">ลบ</a>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
        <div style="margin-top: 20px; display: flex; gap: 10px;">   
            <a href="/" class="btn-back" style="flex: 1; text-align: center; text-decoration: none; line-height: 40px; background: #ccc; border-radius: 4px; color: black;">กลับหน้าหลัก</a>
        </div>  
    </div>
</body>
</html>