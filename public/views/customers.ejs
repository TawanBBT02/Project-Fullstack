<%- include('partials/header') %>
<%- include('partials/sidebar') %>

<div class="d-flex justify-content-between align-items-center border-bottom pb-2 mb-4">
    <h2 class="h4 fw-bold text-dark">👤 รายชื่อลูกค้าทั้งหมด</h2>
    <a href="/add_customer" class="btn btn-primary rounded-pill px-4 shadow-sm">
        <i class="bi bi-plus-lg me-1"></i> เพิ่มลูกค้าใหม่
    </a>
</div>

<div class="card border-0 shadow-sm" style="border-radius: 12px;">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover mb-0 align-middle">
                <thead class="table-light text-muted">
                    <tr>
                        <th class="ps-4">รหัสลูกค้า</th>
                        <th>ชื่อ-นามสกุล</th>
                        <th>เบอร์โทรศัพท์</th>
                        <th>อีเมล</th>
                        <th class="text-end pe-4">จัดการ</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (customers.length === 0) { %>
                        <tr>
                            <td colspan="5" class="text-center py-4 text-muted">ยังไม่มีข้อมูลลูกค้าในระบบ</td>
                        </tr>
                    <% } else { %>
                        <% customers.forEach(function(cust) { %>
                            <tr>
                                <td class="ps-4 fw-medium text-muted">CUST-<%= String(cust.customer_id).padStart(3, '0') %></td>
                                <td class="fw-bold"><%= cust.first_name %> <%= cust.last_name %></td>
                                <td><%= cust.phone || '-' %></td>
                                <td><%= cust.email || '-' %></td>
                                <td class="text-end pe-4">
                                    <a href="/edit-customer/<%= cust.customer_id %>" class="btn btn-sm btn-outline-warning rounded-pill px-3 me-1">
                                        <i class="bi bi-pencil-square"></i> แก้ไข
                                    </a>
                                    <form action="/delete-customer/<%= cust.customer_id %>" method="POST" class="d-inline" onsubmit="return confirm('คุณแน่ใจหรือไม่ที่จะลบลูกค้ารายนี้? ข้อมูลอุปกรณ์ที่ผูกอยู่จะได้รับผลกระทบ');">
                                        <button type="submit" class="btn btn-sm btn-outline-danger rounded-pill px-3">
                                            <i class="bi bi-trash"></i> ลบ
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        <% }) %>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</div>

<%- include('partials/footer') %>