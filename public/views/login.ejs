<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>เข้าสู่ระบบ | ComRepair</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Prompt:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --main-orange: #ff6b00;
            --sidebar-bg: #2b2d42;
        }
        body { 
            background-color: var(--sidebar-bg); 
            display: flex; 
            align-items: center; 
            justify-content: center; 
            min-height: 100vh; 
            margin: 0; 
            font-family: 'Prompt', sans-serif;
        }
        .login-card { 
            border-radius: 15px; 
            overflow: hidden; 
            box-shadow: 0 10px 30px rgba(0,0,0,0.5); 
            width: 100%; 
            max-width: 900px; 
            background: white; 
        }
        .login-left { 
            background: linear-gradient(135deg, #ff8c00 0%, #ff5e00 100%); 
            color: white; 
            padding: 40px; 
            display: flex; 
            flex-direction: column; 
            justify-content: center; 
            align-items: center; 
            text-align: center; 
        }
        .login-right { padding: 50px; }
        .form-control:focus { 
            border-color: var(--main-orange); 
            box-shadow: 0 0 0 0.25rem rgba(255, 107, 0, 0.25); 
        }
        .btn-primary {
            background-color: var(--main-orange);
            border-color: var(--main-orange);
        }
        .btn-primary:hover {
            background-color: #e66000;
            border-color: #e66000;
        }
        /* ปรับแต่งเพื่อให้ขอบปุ่มดูเนียนไปกับ Input */
        .input-group #togglePassword {
            border-color: #dee2e6;
            color: #6c757d;
        }
        .input-group #togglePassword:hover {
            background-color: transparent;
            color: var(--main-orange);
        }
    </style>
</head>
<body>

<div class="login-card row g-0">
    <div class="col-md-5 login-left d-none d-md-flex">
        <i class="bi bi-tools" style="font-size: 5rem; margin-bottom: 20px;"></i>
        <h2 class="fw-bold">ComRepair Sys</h2>
        <p>ระบบจัดการร้านซ่อมคอมพิวเตอร์ที่ครบวงจรที่สุด ติดตามสถานะได้แบบเรียลไทม์</p>
    </div>
    <div class="col-md-7 login-right">
        <h3 class="fw-bold mb-4 text-dark">เข้าสู่ระบบ</h3>
        
        <form action="/login" method="POST">
            <div class="mb-3">
                <label class="form-label text-muted small fw-bold">อีเมล (Email)</label>
                <div class="input-group">
                    <span class="input-group-text bg-light"><i class="bi bi-envelope"></i></span>
                    <input type="email" class="form-control shadow-none" name="email" placeholder="example@email.com" required>
                </div>
            </div>
            <div class="mb-4">
                <label class="form-label text-muted small fw-bold">รหัสผ่าน (Password)</label>
                <div class="input-group">
                    <span class="input-group-text bg-light"><i class="bi bi-lock"></i></span>
                    <input type="password" class="form-control shadow-none" name="password" id="password" placeholder="••••••••" required>
                    <button class="btn btn-outline-secondary border-start-0 bg-light shadow-none" type="button" id="togglePassword">
                        <i class="bi bi-eye" id="eyeIcon"></i>
                    </button>
                </div>
            </div>
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div class="form-check">
                    <input type="checkbox" class="form-check-input" id="remember">
                    <label class="form-check-label text-muted small" for="remember">จดจำฉันไว้</label>
                </div>
                <a href="/forgot_password" class="text-decoration-none fw-bold small" style="color: var(--main-orange);">ลืมรหัสผ่าน?</a>
            </div>
            
            <button type="submit" class="btn btn-primary w-100 py-2 mb-3 fw-bold rounded-pill shadow-sm">เข้าสู่ระบบ</button>
            <p class="text-center text-muted mb-0 small">ยังไม่มีบัญชีใช่ไหม? <a href="/signup" class="text-decoration-none fw-bold" style="color: var(--main-orange);">สมัครสมาชิกที่นี่</a></p>
        </form>
    </div>
</div>
<script>
    const togglePassword = document.querySelector('#togglePassword');
    const password = document.querySelector('#password');
    const eyeIcon = document.querySelector('#eyeIcon');

    togglePassword.addEventListener('click', function (e) {
        // สลับประเภทระหว่าง password และ text
        const type = password.getAttribute('type') === 'password' ? 'text' : 'password';
        password.setAttribute('type', type);
        
        // สลับไอคอนระหว่าง eye และ eye-slash
        eyeIcon.classList.toggle('bi-eye');
        eyeIcon.classList.toggle('bi-eye-slash');
    });
</script>   
</body>
</html>