<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>แก้ไขข้อมูลอุปกรณ์</title>
</head>
<body>
    <div class="container">
        <h1>🔧 แก้ไขข้อมูลอุปกรณ์</h1>
        <p>แก้ไขรายละเอียดอุปกรณ์ของลูกค้า (ID: <%= device.device_id %>)</p>
        
        <form action="/device/update/<%= device.device_id %>" method="POST">
            <label>เจ้าของอุปกรณ์</label>
            <select name="customer_id" required style="width: 100%; padding: 10px; margin-bottom: 15px; border-radius: 5px;">
                <% customers.forEach(customer => { %>
                    <option value="<%= customer.customer_id %>" 
                        <%= (customer.customer_id == device.customer_id) ? 'selected' : '' %>>
                        ID: <%= customer.customer_id %> - <%= customer.first_name %> <%= customer.last_name %>
                    </option>
                <% }) %>
            </select>

            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                <div>
                    <label>ประเภทอุปกรณ์</label>
                    <input type="text" name="device_type" value="<%= device.device_type %>" required>
                </div>
                <div>
                    <label>ยี่ห้อ (Brand)</label>
                    <input type="text" name="brand" value="<%= device.brand %>" required>
                </div>
            </div>

            <label>รุ่น (Model)</label>
            <input type="text" name="model" value="<%= device.model %>" required>
            
            <label>เลขที่ Serial Number</label>
            <input type="text" name="serial_number" value="<%= device.serial_number %>" required>

            <div style="margin-top: 20px; display: flex; gap: 10px;">
                <button type="submit" style="flex: 2; background-color: #f59e0b; color: white; border: none; cursor: pointer; padding: 10px; border-radius: 5px;">💾 บันทึกการแก้ไข</button>
                <a href="/devices-list" class="btn-back" style="flex: 1; text-align: center; text-decoration: none; line-height: 40px; background-color: #64748b; color: white; border-radius: 5px;">ยกเลิก</a>
            </div>
        </form>
    </div>
</body>
</html>