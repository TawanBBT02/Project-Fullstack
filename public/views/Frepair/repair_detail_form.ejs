<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>บันทึกรายละเอียดการซ่อม</title>
</head>
<body>
    <div class="container">
        <h1>🛠 บันทึกรายละเอียดการซ่อม (Repair Details)</h1>
        <p>รหัสงานซ่อมหลัก: <strong><%= repair_id %></strong></p>
        
        <form action="/api/repair-details" method="POST">
            <input type="hidden" name="repair_id" value="<%= repair_id %>">

            <label>ผลการวินิจฉัย (Diagnostic Result)</label>
            <textarea name="diagnostic_result" rows="3" placeholder="ระบุสิ่งที่ตรวจพบจากการเช็คเครื่อง..." required></textarea>

            <label>รายละเอียดการดำเนินการ (Details)</label>
            <textarea name="details" rows="3" placeholder="ระบุสิ่งที่ช่างได้ลงมือทำ..." required></textarea>

            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                <div>
                    <label>วันที่ดำเนินการเสร็จ (Completed Date)</label>
                    <input type="date" name="completed_date" required style="width: 100%; padding: 10px;">
                </div>
                <div>
                    <label>ผลการซ่อม (Repair Result)</label>
                    <select name="repair_result" required style="width: 100%; padding: 10px;">
                        <option value="Success">✅ Success (สำเร็จ)</option>
                        <option value="Failed">❌ Failed (ไม่สำเร็จ)</option>
                    </select>
                </div>
            </div>

            <div style="margin-top: 25px; display: flex; gap: 10px;">
                <button type="submit" style="flex: 2; background-color: #ea580c;">💾 บันทึกรายละเอียดรายการนี้</button>
                <a href="/admin/repair-detail/<%= repair_id %>" class="btn-back" style="flex: 1; text-align: center; text-decoration: none; line-height: 40px;">ยกเลิก</a>
            </div>
        </form>
    </div>
</body>
</html>