<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>เปิดใบสั่งซ่อมใหม่</title>
</head>
<body>
    <div class="container">
        <h1>📑 เปิดใบสั่งซ่อมใหม่ (New Repair Order)</h1>
        <form action="/add-repair" method="POST">
            
            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                <div>
                    <label>เลือกอุปกรณ์ที่ซ่อม (Device)</label>
                    <select name="device_id" required style="width: 100%; padding: 10px;">
                        <option value="">-- เลือกเครื่องซ่อม --</option>
                        <% if (typeof devices !== 'undefined') { %>
                            <% devices.forEach(dev => { %>
                                <option value="<%= dev.device_id %>">
                                    [ID: <%= dev.device_id %>] <%= dev.brand %> <%= dev.model %> 
                                </option>
                            <% }) %>
                        <% } %>
                    </select>
                </div>
                
                <div>
                    <label>มอบหมายช่าง (Technician)</label>
                    <select name="technician_id" required style="width: 100%; padding: 10px;">
                        <option value="">-- เลือกช่างผู้รับผิดชอบ --</option>
                        <% if (typeof technicians !== 'undefined') { %>
                            <% technicians.forEach(tech => { %>
                                <option value="<%= tech.technician_id %>">
                                    <%= tech.first_name %> <%= tech.last_name %>
                                </option>
                            <% }) %>
                        <% } %>
                    </select>
                </div>
            </div>

            <hr style="margin: 20px 0;">

            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                <div>
                    <label>ประเภทอาการ (Problem Type)</label>
                    <select name="problem_type" required style="width: 100%; padding: 10px;">
                        <option value="Hardware">Hardware (ฮาร์ดแวร์)</option>
                        <option value="Software">Software (ซอฟต์แวร์)</option>
                        <option value="Cleaning">Cleaning (ทำความสะอาด)</option>
                        <option value="Other">อื่นๆ</option>
                    </select>
                </div>
                <div>
                    <label>สถานะงาน (Status)</label>
                    <select name="status" required style="width: 100%; padding: 10px;">
                        <option value="Pending">Pending (รอคิว)</option>
                        <option value="In Progress">In Progress (กำลังซ่อม)</option>
                        <option value="Completed">Completed (ซ่อมเสร็จ)</option>
                    </select>
                </div>
            </div>

            <label>วันที่รับเครื่อง (Receive Date)</label>
            <input type="date" name="receive_date" required style="width: 100%; padding: 10px; margin-bottom: 15px;">

            <label>รายละเอียดปัญหา (Issue Description)</label>
            <textarea name="issue_description" rows="4" placeholder="ระบุอาการเสียโดยละเอียด..." required style="width: 100%; padding: 10px; border-radius: 5px;"></textarea>

            <div style="margin-top: 20px; display: flex; gap: 10px;">
                <button type="submit" style="flex: 2; background-color: #ea580c; color: white; border: none; padding: 10px; cursor: pointer; border-radius: 5px;">🚀 บันทึกเปิดงานซ่อม</button>
                <a href="/dashboard" class="btn-back" style="flex: 1; text-align: center; text-decoration: none; line-height: 40px;">กลับหน้าหลัก</a>
            </div>
        </form>
    </div>
</body>
</html>