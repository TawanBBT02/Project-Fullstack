<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>รายละเอียดใบงานซ่อม #<%= repair.repair_id %></title>
</head>
<body>
    <div class="container" style="max-width: 900px;">
        <h1>📄 รายละเอียดใบงานซ่อม #<%= repair.repair_id %></h1>

        <div class="detail-card">
            <div class="form-grid" style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                <div class="detail-row">
                    <h3>👤 ข้อมูลลูกค้า</h3>
                    <% if (repair.device && repair.device.customer) { %>
                        <strong>ชื่อ-นามสกุล:</strong> 
                        <p>คุณ<%= repair.device.customer.first_name %> <%= repair.device.customer.last_name %></p>
                        <strong>เบอร์โทรศัพท์:</strong> 
                        <p><%= repair.device.customer.phone %></p>
                    <% } else { %>
                        <p style="color: red;">! ไม่พบข้อมูลลูกค้า</p>
                    <% } %>
                </div>
                <div class="detail-row">
                    <h3>💻 ข้อมูลอุปกรณ์</h3>
                    <% if (repair.device) { %>
                        <strong>ยี่ห้อ/รุ่น:</strong> 
                        <p><%= repair.device.brand %> <%= repair.device.model %></p>
                        <strong>เลข Serial:</strong> 
                        <p><%= repair.device.serial_number %></p>
                    <% } else { %>
                        <p style="color: red;">! ไม่พบข้อมูลอุปกรณ์</p>
                    <% } %>
                </div>
            </div>

            <hr style="border: 0; border-top: 1px solid #ddd; margin: 25px 0;">

            <div class="detail-row">
                <h3>⏳ ข้อมูลการติดตามงาน</h3>
                <div class="form-grid" style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                    <div>
                        <strong>📅 วันที่รับซ่อม:</strong>
                        <p><%= repair.receive_date %></p>
                    </div>
                    <div>
                        <strong>👨‍🔧 ช่างผู้รับผิดชอบ:</strong>
                        <p><%= repair.technician ? (repair.technician.first_name + ' ' + repair.technician.last_name) : 'ยังไม่มอบหมาย' %></p>
                    </div>
                </div>
                
                <div class="form-grid" style="margin-top: 15px;">
                    <div>
                        <strong>📊 สถานะการซ่อม:</strong>
                        <div style="margin-top: 5px;">
                            <span class="status <%= repair.status === 'Completed' ? 'success' : 'progress' %>">
                                <%= repair.status %>
                            </span>
                        </div>
                    </div>
                </div>
            </div>

            <hr style="border: 0; border-top: 1px solid #ddd; margin: 25px 0;">

            <div class="detail-row">
                <h3>🛠 รายละเอียดการซ่อม</h3>
                <strong>อาการที่แจ้ง (Issue):</strong>
                <p style="background: #fff7ed; border-left: 4px solid #ea580c; padding: 15px; margin-top: 10px;">
                    <%= repair.issue_description || "ไม่มีรายละเอียดอาการ" %>
                </p>
                
                <div class="form-grid" style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-top: 20px;">
                    <div>
                        <strong>🛠 ประเภทงาน:</strong>
                        <p><%= repair.problem_type %></p>
                    </div>
                    <!-- <div>
                        <strong>💳 สถานะการเงิน:</strong>
                        <div style="margin-top: 5px;">
                            <span class="status <%= repair.payment_id ? 'success' : 'failed' %>" 
                                  style=" <%= !repair.payment_id ? 'background:#64748b' : '' %>">
                                <%= repair.payment_id ? 'ชำระเงินแล้ว' : 'รอการชำระ' %>
                            </span>
                        </div> -->
                    </div>
                </div>
            </div>
        </div>

        <div style="margin-top: 30px; display: flex; gap: 15px; justify-content: center; flex-wrap: wrap;">
            <a href="/dashboard" class="btn-back" style="padding: 12px 25px; text-decoration: none; background: #64748b; color: white; border-radius: 5px;">⬅ กลับไป Dashboard</a>
            <a href="/repair/update/<%= repair.repair_id %>" class="btn-detail" style="background:#f59e0b; padding: 12px 25px; text-decoration: none; color: white; border-radius: 5px;">📝 แก้ไขสถานะงาน</a>
        </div>
    </div>
</body>
</html>